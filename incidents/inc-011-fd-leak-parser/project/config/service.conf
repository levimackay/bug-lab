# Orders API runtime configuration
# Reloaded on a timer; keep keys stable, ops scripts grep this file.
service_name = orders-api
port = 8080
log_level = info
max_connections = 200
request_timeout_ms = 5000
